import { ProblemDifficultyPipe } from './problem-difficulty.pipe';

describe('ProblemDifficultyPipe', () => {
  it('create an instance', () => {
    const pipe = new ProblemDifficultyPipe();
    expect(pipe).toBeTruthy();
  });
});
