import { SubmissionStatusClassPipe } from './submission-status-class.pipe';

describe('SubmissionStatusClassPipe', () => {
  it('create an instance', () => {
    const pipe = new SubmissionStatusClassPipe();
    expect(pipe).toBeTruthy();
  });
});
